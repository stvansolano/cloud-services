version: '3'
services:
  web_api:
    build:
      context: backend/App
      dockerfile: Dockerfile  
    image: web_api
    environment:
      - SERVICE_PORTS=5000
          
  web_app: 
    build:
      context: frontend
      dockerfile: Dockerfile  
    image: web_app
    environment:
      - SERVICE_PORTS=5001

      
